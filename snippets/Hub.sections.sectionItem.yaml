name: section
label: section
description: Create a `section` item in Hub.sections
body: |2
  - title: ${1:title}
    items:
    - type: ${2:type}
      style: ${3:style}
      className: ${4:className}
      colums: ${5:0}
      tableColums:
      - image:
          src: ${6:src}
      tableRows:
      - title: ${7:title}
        values:
        - href: ${8:href}
      text: ${9:text}
      content: ${10:content}
      items:
      - title: ${11:title}
        content: ${12:content}
        text: ${13:text}
        href: ${14:href}
        image:
          src: ${15:src}
          href: ${16:href}